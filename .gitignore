__pycache__/
*.py[cod]
*$py.class
.DS_Store
*.json
*.pcap
.idea/
.vscode/
build/
dist/
AppDir/
squashfs-root/
*.AppImage
*.tar.gz
*.zip
.venv/
.agents/
scratch/

# Agent/editor tooling and development notes -- not needed to run the app.
# These stay on the developer's disk; they are just kept out of the repo.
.superpowers/
.claude/
.cursorrules
AGENTS.md
ATTRIBUTIONS.md
markdowns/
docs/
WALKTHROUGH.md
.github/copilot-instructions.md

# Unused 3D models. Only pine_tree.glb is loaded by assets/range/js/foliage.js;
# these packs were referenced solely by stale build output and add ~240MB.
# Kept locally in case the range scene ever wants them again.
assets/range/models/trees/five_birch_trees_pack_lowpoly_lods.glb
assets/range/models/trees/maple_trees_pack_lowpoly_game_ready_lods.glb
assets/range/models/trees/realistic_fir_trees_pack_lods_gameready.glb
assets/range/models/trees/realistic_fir_tree_clean.glb
assets/range/models/trees/pine_tree__low_poly_stylized_tree.glb

# Nova IR vision-lab output. Generated by nova_vision_lab.py from local
# captures, and the filenames carry per-user account IDs, so this is user
# data rather than repo content -- it must not be published.
output_user_facing/
output_labeler/

.aider*
